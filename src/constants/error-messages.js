const errorMessages = {
  /**
   * @description If the document is empty or the document is missing.
   */
  emptyFile:
    "Oops...document cannot be blank, please upload a document with content",
  /**
   * @description if the upload document is of the front format and the handler failed to process
   */
  unsupportedFileFormat:
    "Oops...It appears your document is not properly formatted.",
};

module.exports = errorMessages;
